[project]
name = "fsae-car-model"
version = "0.1.0"
description = "FSAE vehicle dynamics — kinematics and parameter sweep"
requires-python = ">=3.11"
dependencies = [
    "numpy>=2.0",
    "scipy>=1.13",
    "matplotlib>=3.9",
    "pandas>=2.2",
]

[tool.uv]
dev-dependencies = []
